import projects from "../components/projects-page/projects.reducer";
import cv from "../components/cv/edit-page.reducer";

const rootReducer = {
  projects,
  cv,
};

export default rootReducer;